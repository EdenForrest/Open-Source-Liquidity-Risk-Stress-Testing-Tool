# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/
ENV/

# Output files
output/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local

# Pytest
.pytest_cache/
.coverage
htmlcov/

# ── Data files — never commit raw or real data ──────────────────────────────
# All CSV/Excel/JSON in data/ are local only; use data/sample/ for public demos
data/*.csv
data/*.xlsx
data/*.json
data/real/
data/cache/
# Allow the synthetic sample directory (synthetic demo data only — safe to share)
# Tracks synthetic holdings/NAV and synthetic market data so the download-sample-data
# button can bundle them off a fresh clone. The yield curve and the fetch-errors log
# are NOT part of the public download, so keep them out of git.
!data/sample/
!data/sample/**
data/sample/zero_coupon_yields.xlsx
data/sample/market_data_ERRORS.csv

# Generated output reports and charts
output/
liquidity_risk_tool/output/

# Refinitiv / LSEG licensed data — keep private
# The extractor code lives in integrations/refinitiv/ (safe to commit)
# but its outputs, cache files, and any real data must never be committed.
data/fetch_market_data.py
data/README_fetch_market_data.md
data/market_data_cache.csv
data/market_data_ALL.csv
data/market_data_ERRORS.csv
integrations/refinitiv/.env
integrations/refinitiv/secrets.py

# Internal documentation (not for public release)
docs/

# Local tooling
.claude/
liquidity_risk_tool/.claude/

# Vite build cache
frontend/.vite/

# Node dependencies — install via npm, never commit
node_modules/

# Generated documents (built from MODEL.md via scripts/md2html.mjs)
MODEL.html
MODEL.pdf

# graphify knowledge-graph output — local tooling artifact
graphify-out/
