# Copy to .env.local for local dev — never commit .env.local
# Leave blank in dev (Vite proxy handles it)
# In Vercel: set to your Render backend URL
VITE_API_BASE_URL=https://liquidity-risk-api.onrender.com
